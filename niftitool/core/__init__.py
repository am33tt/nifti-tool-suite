"""Pure-Python scientific core.

No tkinter, no matplotlib — every function in this sub-package is safely
callable from a headless context (scripts, notebooks, future C++ bindings,
unit tests). Each module maps to one concept and is documented for the
future documentation site.
"""
